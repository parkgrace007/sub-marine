import React from 'react'

/**
 * CurrentBar - Price Display Component
 *
 * Displays current BTC/USDT price and 24h change percentage.
 * Previously contained Bull/Bear gradient - now moved to MainVisualizationSet for full-height display.
 */

function CurrentBar({ sentiment = {} }) {
  // Price display removed per user request
  return null
}

export default React.memo(CurrentBar)
